# AGENTS.md — سند تداوم پروژه‌ی مناد

این سند برای این نوشته شده که **هر** توسعه‌دهنده یا هوش مصنوعی بتواند پروژه را بدون هیچ دانش قبلی، از همین نقطه ادامه دهد. اگر تو یک ایجنت هستی: اول این سند را کامل بخوان، بعد `docs/adr/ADR.md` را، بعد کد را.

## ۱) پروژه چیست
**مناد (MONAD)** — موتور گفتگوی انسان با حقیقت. شبکه‌ی اجتماعی نیست. انسان‌ها با هم گفتگو نمی‌کنند؛ تنها گفتگو «انسان ↔ مناد» است. کاربر در پایان تصمیم می‌گیرد گفتگو منتشر شود (خواندنی برای همه‌ی جهان) یا خصوصی بماند. تنها تعامل دیگران: «ارزشمند بود / نبود».

دامنه‌ی استقرار: **asraar.ir** · مخزن: **github.com/aghaie/asraar** · زیرساخت هدف: **آروان‌کلاد**

## ۲) اصول تغییرناپذیر (از مالک پروژه — هرگز نقض نکن)
1. **مرجع حقیقت فقط قرآن است.** نه تفسیر، نه فقه، نه روایت، نه اجماع، نه فلسفه، نه عرف. آیه با آیه فهمیده می‌شود (انسجام درون‌متنی). اگر چند برداشت معتبر از متن ممکن است، همه بیان می‌شوند با دلیل. چیزی که از متن برنمی‌آید به قرآن نسبت داده نمی‌شود.
2. **صداقت معرفتی:** «نمی‌دانم» و «قرآن پاسخ قطعی به این پرسش نمی‌دهد» پاسخ‌های مشروع و لازم‌اند. حقیقت همیشه بر رضایت کاربر مقدم است.
3. **روش سقراطی:** رشد فهم کاربر، نه پاسخ‌دهی. گاهی بهترین پاسخ یک سوال بهتر است.
4. **هیچ سازوکار شبکه‌ی اجتماعی ساخته نمی‌شود:** کامنت، ریپلای، منشن، دایرکت، فالو، چت کاربر با کاربر، گروه — هرگز.
5. **الگوریتم دیده‌شدن فقط از کیفیت/عمق/اثر معرفتی تغذیه می‌شود.** داده‌ی شهرت/فالوئر/پول نباید حتی در schema وجود داشته باشد.
6. **مناد رایگان است. برای همیشه.** تأمین مالی فقط از هدیه‌ی داوطلبانه‌ی مردم (خودکفا). تبلیغ، فروش داده و اشتراک اجباری ممنوع ابدی.
7. **مناد هیچ‌کس را بلاک نمی‌کند. همیشه هست.** ضد اسپم فقط «سهمیه‌ی روزانه» است که فردا از نو باز می‌شود. محرومیت دائمی نساز.
8. **چندزبانگی دوطرفه:** هر کس به زبان خودش با مناد حرف می‌زند و هر خواننده گفتگوها را به زبان خودش می‌خواند.
9. **نگارش‌پیرایی بدون ذره‌ای تغییر معنا** و همیشه با حفظ متن اصلی قابل مشاهده.
10. **اخبار:** مناد در جریان رویدادها قرار می‌گیرد ولی در قضاوت خطا نمی‌کند — تمایز رخداد/روایت/داوری؛ بدون حکم بی‌بیّنه؛ معیار داوری فقط قرآن.
11. **UI حداقل‌گرا و آرام؛ بدون Dark Pattern و اعتیاد بصری.** قهرمان سایت حقیقت است، نه کاربران.
12. **هویت: بدون ثبت‌نام اجباری؛ کمترین اصطکاک.**

## ۳) وضعیت فعلی (تیر ۱۴۰۵ / ژوئیه ۲۰۲۶) — MVP کامل و سبز
- ✅ کل چرخه کار می‌کند: شروع گفتگو → پیام (LLM) → انتشار/خصوصی → فهرست رتبه‌بندی‌شده → سیگنال ارزش → ترجمه
- ✅ ۳۰ تست (`npm test`)، بیلد تولیدی (`npm run build`)، آزمون دود E2E روی سرور واقعی انجام شده
- ✅ Dockerfile آماده (standalone، non-root، volume=/data)
- ⬜ دیزاین نهایی: مالک با Claude Design طراحی می‌کند (پرامپت: `docs/design-brief-prompt.md`) و تحویل می‌دهد — فقط توکن‌های `globals.css` و کلاس‌های پایه عوض شوند
- ⬜ استقرار روی آروان + اتصال دامنه (راهنما: `docs/DEPLOYMENT.md`)

## ۴) معماری در یک بند
Clean Architecture در یک Monolith ماژولار Next.js 15: `src/core` (دامنه + پورت‌ها + usecaseها؛ خالص و framework-free) ← `src/infrastructure` (SQLite با `node:sqlite`، آداپتر Anthropic بدون SDK، مترجم، DI در `container.ts`) ← `src/app` (صفحات RTL فارسی + Route Handlerها). **شخصیت مناد فقط در `src/infrastructure/llm/monad-system-prompt.ts` است** — مهم‌ترین فایل پروژه. جزئیات: `docs/ARCHITECTURE.md`، تصمیم‌ها و دلایل: `docs/adr/ADR.md`.

## ۵) قراردادهای کار
- TypeScript strict؛ تست برای هر منطق دامنه (vitest، `tests/`)؛ کامنت‌ها و UI فارسی، شناسه‌ها انگلیسی
- هر تصمیم معماری تازه → یک ADR در `docs/adr/ADR.md`
- هر تغییر schema → migration جدید در `src/infrastructure/db/migrations.ts` (نسخه‌ی بعدی؛ هرگز migration قبلی را ویرایش نکن)
- Core هرگز import از `next/*`، `node:sqlite` یا `fetch` مستقیم نداشته باشد — فقط پورت
- قبل از ادعای اتمام: `npm test && npm run build` سبز باشد

## ۶) نقشه‌ی راه پس از MVP (به ترتیب پیشنهادی)
1. **اعمال دیزاین نهایی** خروجی Claude Design روی توکن‌ها/کلاس‌ها
2. **استقرار آروان + دامنه + پشتیبان‌گیری روزانه‌ی SQLite**
3. **Streaming پاسخ مناد** (SSE) برای تجربه‌ی آرام‌تر
4. **صفحه‌ی هدیه** (تأمین مالی داوطلبانه — اصل ۶؛ درگاه ایرانی مثل زرین‌پال)
5. **ذخیره‌ی زبان گفتگو + نمایش خودکار به زبان مرورگر خواننده** (`Accept-Language`)
6. **جست‌وجو در گفتگوهای منتشرشده** (FTS5 خود SQLite)
7. **پورت `NewsProvider`** برای «در جریان اخبار بودن» با قیود اصل ۱۰
8. **مقیاس:** Postgres برای Repository، Redis برای RateLimiter، صف ترجمه — فقط آداپتر جدید، Core دست نمی‌خورد
9. **Audit log و Observability عمیق‌تر** (اکنون: لاگ JSON ساخت‌یافته)

## ۷) هشدارهای عملیاتی
- `MONAD_SALT` تولیدی باید تصادفی و ثابت باشد (تغییرش سهمیه‌ها و یکتایی رأی را ریست می‌کند)
- سرور ایران ممکن است به `api.anthropic.com` نرسد → `ANTHROPIC_BASE_URL` را به relay بده
- SQLite تک‌نویسنده است → فقط یک replica تا قبل از مهاجرت DB
- پشت CDN/پراکسی، هدر `X-Forwarded-For` باید IP واقعی را بدهد وگرنه سهمیه‌ها مشترک می‌شوند

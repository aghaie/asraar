# AGENTS.md — سند تداوم پروژه‌ی مناد

این سند برای این نوشته شده که **هر** توسعه‌دهنده یا هوش مصنوعی بتواند پروژه را بدون هیچ دانش قبلی، از همین نقطه ادامه دهد. اگر تو یک ایجنت هستی: اول این سند را کامل بخوان، بعد `docs/adr/ADR.md` را، بعد کد را.

## ۱) پروژه چیست
**مناد (MONAD)** — موتور گفتگوی انسان با حقیقت. شبکه‌ی اجتماعی نیست. انسان‌ها با هم گفتگو نمی‌کنند؛ تنها گفتگو «انسان ↔ مناد» است. کاربر در پایان تصمیم می‌گیرد گفتگو منتشر شود (خواندنی برای همه‌ی جهان) یا خصوصی بماند. تنها تعامل دیگران: «ارزشمند بود / نبود».

دامنه‌ی استقرار: **asraar.ir** · مخزن: **github.com/aghaie/asraar** · زیرساخت هدف: **آروان‌کلاد**

## ۲) اصول تغییرناپذیر (از مالک پروژه — هرگز نقض نکن)
> **سند حاکم: [منشور معرفتی مناد](docs/charter.md).** این منشور جایگزین تمام تعاریف پیشین است و بر همه‌ی اجزای سیستم (پرامپت، رفتار مدل، معماری استدلال، UX، منطق تصمیم) حاکم است. پیاده‌سازی رفتاری‌اش در `src/infrastructure/llm/monad-system-prompt.ts` است. بندهای زیر خلاصه‌ی عملیاتی آن‌اند؛ در تعارض، متن منشور معیار است.

1. **بنیان معرفتی فقط قرآن است.** نه تفسیر، نه فقه، نه روایت، نه اجماع، نه فلسفه، نه عرف — و نه هیچ دستگاه فکری بیرونی (دیالکتیک، لیبرالیسم، اومانیسم، ماتریالیسم، روان‌شناسی مدرن…) و نه الگوهای پیش‌فرض مدل‌های زبانی. آیه با آیه فهمیده می‌شود. مناد نخست از این بنیان می‌اندیشد، سپس به زبان طبیعی پاسخ می‌دهد — نه اینکه مثل یک LLM رایج بیندیشد و پاسخ را با مفاهیم قرآنی بیاراید.
2. **اصل بر فهم است، نه نقل‌قول.** مناد به‌طور پیش‌فرض آیه/سوره نقل نمی‌کند؛ اگر پاسخ بر پایه‌ی مفهومی قرآنی باشد، همان پاسخ روان کافی است. استناد دقیق فقط وقتی کاربر دلیل/مستند/بررسی عمیق‌تر بخواهد. (ADR-0015)
3. **صداقت و اصلاح‌پذیری:** «نمی‌دانم» و «قرآن پاسخ قطعی نمی‌دهد» پاسخ‌های مشروع‌اند؛ چند احتمال معتبر از هم تفکیک می‌شوند. هیچ پاسخی مقدس نیست؛ اعتبار مناد از صداقت در اصلاح اشتباه است، نه از اشتباه‌نکردن. حقیقت همیشه بر رضایت کاربر مقدم است.
4. **روش سقراطی و معیار موفقیت:** هدف، عمیق‌تر/شفاف‌تر/صادقانه‌تر شدن فهم انسان است، نه قانع شدن او یا پایان گفتگو — حتی اگر نتیجه، پذیرش ندانستن باشد. گاهی بهترین پاسخ یک سوال بهتر است.
5. **هیچ سازوکار شبکه‌ی اجتماعی ساخته نمی‌شود:** کامنت، ریپلای، منشن، دایرکت، فالو، چت کاربر با کاربر، گروه — هرگز.
6. **الگوریتم دیده‌شدن فقط از کیفیت/عمق/اثر معرفتی تغذیه می‌شود.** داده‌ی شهرت/فالوئر/پول نباید حتی در schema وجود داشته باشد.
7. **مناد رایگان است. برای همیشه.** تأمین مالی فقط از هدیه‌ی داوطلبانه‌ی مردم (خودکفا). تبلیغ، فروش داده و اشتراک اجباری ممنوع ابدی.
8. **مناد هیچ‌کس را بلاک نمی‌کند. همیشه هست.** ضد اسپم فقط «سهمیه‌ی روزانه» است که فردا از نو باز می‌شود. محرومیت دائمی نساز.
9. **چندزبانگی دوطرفه:** هر کس به زبان خودش با مناد حرف می‌زند و هر خواننده گفتگوها را به زبان خودش می‌خواند.
10. **نگارش‌پیرایی بدون ذره‌ای تغییر معنا** و همیشه با حفظ متن اصلی قابل مشاهده.
11. **اخبار:** مناد در جریان رویدادها قرار می‌گیرد ولی در قضاوت خطا نمی‌کند — تمایز رخداد/روایت/داوری؛ بدون حکم بی‌بیّنه؛ معیار داوری فقط قرآن.
12. **UI حداقل‌گرا و آرام؛ بدون Dark Pattern و اعتیاد بصری.** قهرمان سایت حقیقت است، نه کاربران.
13. **هویت: بدون ثبت‌نام اجباری؛ کمترین اصطکاک.**
14. **کرامتِ فضای عمومی (ADR-0021):** انتشار عمومی از توهین/هرزگی/نفرت/اسپم پاک می‌ماند («وَقُولُوا لِلنَّاسِ حُسْنًا»)، اما **پرسیدن و خصوصی‌ماندن هرگز داوری نمی‌شوند** و شک و پرسشِ سختِ صادقانه هرگز سرکوب نمی‌شود («لا اکراه»). داوری فقط بر نوشته‌ی جوینده و فقط هنگام انتشار است؛ در تردید، اجازه (fail-open).

### سادگی و تأخیر در انتزاع (ADR-0025 — حاکم بر همه‌ی تصمیم‌ها)
- **پرسشِ نخستِ هر تغییر:** «آیا فهمِ انسان را به حقیقت نزدیک‌تر می‌کند، یا فقط سیستم را پیچیده‌تر/جذاب‌تر/سوشال‌تر؟» اگر روشن نیست، وارد هسته نکن.
- **سادگی اصلِ معماری است.** هر قابلیت باید ضرورتش را نسبت به مأموریت اثبات کند. مناد نباید انبوهی از قابلیت‌های جذابِ بی‌ربط شود.
- **تأخیر در انتزاع:** هیچ موجودیتِ دامنه‌ای پیش‌دستانه (به‌خاطرِ جذابیت/آینده) وارد هسته نشود؛ نخست باید در داده‌ی واقعی خود را اثبات کند. `Question`/`Insight`/گرافها/پایپلاینِ ماژولار فقط چشم‌اندازند (نه هسته) تا اثباتِ نیاز.
- **قابلیتِ تازه فقط اگر مستقیم به مأموریت کمک کند و زیرساختش موجود باشد.** ایده‌های دیگر → بخشِ «Future Ideas» نقشه‌ی راه.

### معماری معرفتی (ADR-0022 — سندِ حاکم: [docs/epistemic-architecture.md](docs/epistemic-architecture.md))
15. **گفتگو مادهٔ خام است، نه داراییِ اصلی.** هدفِ سیستم: افزایشِ فهمِ انسان. داراییِ واقعی: نقشهٔ فهم (Questions/Concepts/Insights). معیارِ هر تصمیم: «آیا به دقیق‌ترشدنِ این نقشه کمک می‌کند؟»
16. **شاخه = «این مسیر را ادامه بده»، نه مخالفت.** شاخه خنثی است: عمیق‌تر کردن، فرضِ دیگر، مثالِ تازه، زاویهٔ نو — حتی هنگام موافقت. هرگز آن را «مخالفت/رأی منفی» ننام.
17. **فقط اثرِ معرفتی سنجیده می‌شود، نه محبوبیت.** سیگنال‌ها: «بیشتر فکر کردم / فهمم بیشتر شد / مبنای ادامه‌ام شد / شاخه ساختم / پس‌زمینه را خواندم / بعداً بازگشتم». valueUp/valueDownِ محبوبیت‌محور نسخ شد.
18. **پاسخِ سه‌لایه:** کوتاه و روشن → استدلال → مبنای قرآنی؛ تنبل و کش‌شونده.
19. **پاسخِ ترجمه ویژگیِ واحدِ محتواست:** کلیدِ `(contentType, contentId, lang)`. (موجودیت‌های `Question`/`Insight` و پایپلاینِ ماژولار طبق ADR-0025 به «Future Ideas» موکول شدند — رجوع به بخش ۶ب.)

## ۰) از این‌جا ادامه بده (برای هر جلسه‌ی تازه)
> وقتی مالک می‌گوید «ادامه بده»، این بک‌لاگ را از بالا بردار. قبل از هر کار: `npm test` سبز، و برای بیلد فقط `npm run verify-build`. پس از هر قدم: commit با پیام فارسی + push روی `monad-mvp`.

**بک‌لاگِ UI/UX (دورِ اول — انجام‌شده در ee75ea7):**
- [x] «هدیه» از nav به `/about` منتقل شد.
- [x] سوییچرِ زبان: فارسی اول، فقط `fa/ar/en` (بقیه مخفی، در `VISIBLE_LANGS`)، فقط آیکن، هم‌طرازِ nav.
- [x] صفحه‌ی گفتگو: سلکتِ زبان حذف؛ زبان/جهت از هدر (`currentLocale`).
- [x] صفحه‌ی اصلی: جست‌وجو و بازه‌ی تاریخ مینیمال/آیکنی روبروی عنوان (`.page-head` + `.head-tool`).
- [x] تاریخ‌ها: `src/lib/format-date.ts` — فارسی شمسی، سایر میلادی.
- [x] صفحه‌ی `/updates` از `docs/telegram-posts.md` + لینک در فوتر.

**بک‌لاگِ UI/UX (دورِ دوم — انجام‌شده):**
- [x] سوییچرِ زبان از هدر به **فوتر** منتقل شد (کپی‌رایت راست، زبان چپ، منوی رو به بالا `.lang-switch.up`). گرافیکِ زیباتر (پیل با آیکنِ کره + نامِ زبان).
- [x] سه کنشِ پاسخِ مناد (استدلال/مبنای قرآنی/«این مسیر را ادامه بده») به **آیکنِ درونِ باکس، پایین-چپ** تبدیل شد (`.msg-actions`/`.msg-act` + `.layer-panel`). آیکن‌ها: `ReasoningIcon/QuranIcon/BranchIcon`.
- [x] **پنل مدیریت (ADR-0026):** `/admin` (داشبورد) + `/admin/conversations` (نظارتِ محتوا: خارج‌از‌انتشار/انتشارِ دوباره/حذف) + `/admin/health` (سلامتِ پیکربندیِ بی‌راز برای استقرار). احراز با env `MONAD_ADMIN_EMAILS`. بدونِ بلاکِ کاربر (ADR-0006).

**مینی‌اپِ تلگرام (ADR-0027 — انجام‌شده):**
- [x] `@monad_house_bot` → مینی‌اپِ `/tma` درونِ همان اپ Next.js، با تمِ **دقیقاً بومیِ تلگرام** (بازنگاریِ توکن‌های مناد به `--tg-*` در `tma.css`، حباب‌های چت، هدر/فوترِ مخفی).
- [x] تلگرام = روشِ ورودِ سومِ هویت (کنارِ ایمیل/گوگل): `verifyInitData` خالص (core) + آداپترِ `telegram-oauth.ts` + `POST /api/tma/auth` + migration v8 (`telegram_id`). بازاستفاده‌ی کاملِ `ChatSession` و endpointهای موجود؛ صفر تکثیرِ هسته.
- [x] حلقه‌ی گفتگو به `api.telegram.org` نمی‌زند (اعتبارسنجیِ آفلاین) → بی‌گلوگاهِ فیلترینگ از ایران.
- [ ] **پیش از استقرار:** `TELEGRAM_BOT_TOKEN` را در env تولید بگذار؛ در BotFather دکمه‌ی منوی مینی‌اپ را به `https://asraar.ir/tma` تنظیم کن. (بدونِ توکن، `/api/tma/auth` با ۵۰۳ خاموش است.)

**بازِ باقی‌مانده / گام‌های بعدی:**
- [ ] پیش از استقرار: `MONAD_ADMIN_EMAILS=aghaie.ali@gmail.com` را در env تولید بگذار تا پنل فعال شود.
- [i] ورودِ محلی به پنل: `http://localhost:3000/api/dev/login` (فقط توسعه، ۴۰۴ در production، فقط ایمیل‌های ادمین). در تولید: لینکِ ایمیل/گوگل.
- [ ] پرداختِ بصریِ بیشتر (ریزه‌کاری‌ها) در صورت درخواست → از طریق Claude Design (پروژه `c7de23e8-c8b2-439c-8264-387b419802a4`).
- [ ] استقرار روی asraar.ir (نیازمند دسترسی پنلِ مالک).
- [ ] نقشه‌ی راهِ پنل مدیریت (ADR-0026): کاربران فقط‌خواندنی، تحلیلِ سیگنال‌ها، ویرایشگرِ به‌روزرسانی‌ها — فقط با اثباتِ نیاز.
- [ ] Future Ideas بخش ۶ب (فقط پس از اثباتِ نیاز).


## ۳) وضعیت فعلی (تیر ۱۴۰۵ / ژوئیه ۲۰۲۶) — MVP کامل و سبز
- ✅ کل چرخه کار می‌کند: شروع گفتگو → پیام (LLM) → انتشار/خصوصی → فهرست رتبه‌بندی‌شده → سیگنال ارزش → ترجمه
- ✅ ۳۰ تست (`npm test`)، بیلد تولیدی (`npm run build`)، آزمون دود E2E روی سرور واقعی انجام شده
- ✅ Dockerfile آماده (standalone، non-root، volume=/data)
- ✅ دیزاین نهایی اعمال شد: خروجی Claude Design (پروژه c7de23e8، فونت Vazirmatn متغیر در `public/fonts/`، نشان دایره‌ونقطه، حالت تاریک خودکار) روی `src/app/globals.css` و `layout.tsx` سوار شد. تأیید بصری با اسکرین‌شات انجام شد (ADR-0014)
- ⬜ استقرار روی آروان + اتصال دامنه (راهنما: `docs/DEPLOYMENT.md`)

## ۴) معماری در یک بند
Clean Architecture در یک Monolith ماژولار Next.js 15: `src/core` (دامنه + پورت‌ها + usecaseها؛ خالص و framework-free) ← `src/infrastructure` (SQLite با `node:sqlite`، آداپتر Anthropic بدون SDK، مترجم، DI در `container.ts`) ← `src/app` (صفحات RTL فارسی + Route Handlerها). **شخصیت مناد فقط در `src/infrastructure/llm/monad-system-prompt.ts` است** — مهم‌ترین فایل پروژه. جزئیات: `docs/ARCHITECTURE.md`، تصمیم‌ها و دلایل: `docs/adr/ADR.md`.

## ۵) قراردادهای کار
- TypeScript strict؛ تست برای هر منطق دامنه (vitest، `tests/`)؛ کامنت‌ها و UI فارسی، شناسه‌ها انگلیسی
- هر تصمیم معماری تازه → یک ADR در `docs/adr/ADR.md`
- هر تغییر schema → migration جدید در `src/infrastructure/db/migrations.ts` (نسخه‌ی بعدی؛ هرگز migration قبلی را ویرایش نکن)
- Core هرگز import از `next/*`، `node:sqlite` یا `fetch` مستقیم نداشته باشد — فقط پورت
- قبل از ادعای اتمام: `npm test && npm run build` سبز باشد
- **پس از هر پیشرفت مهم، یک پست ساده و همه‌فهم به بالای `docs/telegram-posts.md` اضافه کن** (برای کانال تلگرام مناد — مردم در جریان پیشرفت باشند)

## ۶) نقشه‌ی راه پس از MVP (به ترتیب پیشنهادی)
1. ~~اعمال دیزاین نهایی~~ ✅ انجام شد (ADR-0014)
2. **استقرار آروان + دامنه + پشتیبان‌گیری روزانه‌ی SQLite** (تنها قدم بزرگِ باقی‌مانده؛ نیازمند دسترسی پنل آروان)
3. ~~Streaming پاسخ مناد (SSE)~~ ✅ انجام شد (ADR-0013)
4. ~~صفحه‌ی هدیه~~ ✅ صفحه‌ی `/gift` با `MONAD_GIFT_URL` (ADR-0018)؛ **یکپارچگی کامل درگاه زرین‌پال با تأیید callback هنوز مانده**
5. ~~زبان خودکار خواننده~~ ✅ انجام شد (ADR-0017)
6. ~~جست‌وجوی گفتگوها (FTS5)~~ ✅ انجام شد (ADR-0016)
7. ~~بازه‌های زمانی صفحه‌ی اصلی~~ ✅ انجام شد (ADR-0020)
8. ~~هویت اختیاری: ورود ایمیل/گوگل + پروفایل خصوصی~~ ✅ انجام شد (ADR-0019)؛ **گوگل نیازمند اعتبارنامه‌ی تولید؛ SMTP برای ایمیلِ واقعی**
9. ~~معماری معرفتی (ADR-0022) — قابلیت‌های زنده~~ ✅ انجام شد:
   - R1 رتبه‌بندیِ اثرِ فهم + سیگنال‌های معرفتی (نسخِ محبوبیت؛ migration v5) · R2 شاخهٔ خنثی «این مسیر را ادامه بده» + درختِ شاخه‌ها (migration v6) · R4 پاسخِ سه‌لایه (کوتاه→استدلال→مبنای قرآنی، تنبل+کش؛ migration v7) · R3 چندزبانگیِ سراسری (سوییچر زبان، locale سه‌منبعی، RTL/LTR، ترجمهٔ محتوای تنبل+کش، کاتالوگ i18n).
   - نکته: `Question`/`Insight` که در R0 پیش‌دستانه ساخته بودم، طبق ADR-0025 (تأخیر در انتزاع) **از هسته حذف شدند**.
10. **استقرار آروان + دامنه** (بزرگ‌ترین قدمِ باقی‌مانده؛ نیازمند دسترسی پنلِ مالک)
11. **باقی‌مانده‌ی روشن:** یکپارچگیِ کاملِ درگاه زرین‌پال (Merchant ID) · اعتبارنامه‌ی گوگل/SMTP برای تولید · تولیدِ کاتالوگِ ۱۱ زبانِ دیگر با `npm run gen-i18n` (فعلاً فقط فارسی کامل، مالک خواست)

## ۶ب) Future Ideas — فقط پس از اثباتِ نیاز در داده‌ی واقعی (ADR-0025؛ وارد هسته نکن)
- **موجودیت‌های معرفتی:** `Question` (پرسش‌های هم‌ریشه)، `Insight` (فهمِ حاصل، دارایی)، و **سه گراف** (Questions/Concepts/Insights). چشم‌انداز در `docs/epistemic-architecture.md`.
- **معماریِ ماژولارِ پاسخ** (Question Analyzer → Concept Resolver → Knowledge Builder → Reasoning → Background → NLG) — تا وقتی پرامپتِ یکپارچه کافی است، انتزاع نکن.
- **پورت `NewsProvider`** (اخبار با قیودِ اصل ۱۱) · **مقیاس** (Postgres/Redis/صف — فقط آداپتر) · **Audit log/Observability عمیق‌تر**.
- معیارِ ورود به هسته: «آیا در استفاده‌ی واقعی، نیازِ پایدار بودنش اثبات شده و مستقیم به مأموریت کمک می‌کند؟»

## ۷) هشدارهای عملیاتی
- `MONAD_SALT` تولیدی باید تصادفی و ثابت باشد (تغییرش سهمیه‌ها و یکتایی رأی را ریست می‌کند)
- دو موتور LLM موجود است: Anthropic و OpenAI (ADR-0012). انتخاب با `MONAD_ENGINE` یا وجود کلید (`ANTHROPIC_API_KEY` / `OPENAI_API_KEY`). محیط توسعه‌ی فعلی مالک با کلید OpenAI کار می‌کند
- سرور ایران ممکن است به `api.anthropic.com` نرسد → `ANTHROPIC_BASE_URL` را به relay بده
- SQLite تک‌نویسنده است → فقط یک replica تا قبل از مهاجرت DB
- پشت CDN/پراکسی، هدر `X-Forwarded-For` باید IP واقعی را بدهد وگرنه سهمیه‌ها مشترک می‌شوند
- **هویت (ADR-0019):** ورود اختیاری است؛ جریان ناشناس هرگز نباید بشکند. ورود ایمیلی بدون SMTP، لینک را در کنسول چاپ می‌کند (فقط توسعه)؛ برای تولید `SMTP_*` لازم است. گوگل بدون `GOOGLE_CLIENT_*` پنهان می‌ماند. `APP_URL` باید در تولید نشانی واقعی (https://asraar.ir) باشد وگرنه لینک جادویی به localhost اشاره می‌کند. کوکی نشست در تولید `Secure` می‌شود (`NODE_ENV=production`)
- **پروفایل خصوصی است:** هرگز نام کاربر یا فهرست شخصی را در صفحات عمومی نشان نده (اصل «قهرمان حقیقت است، نه کاربران»)
- **⚠️ برای راستی‌آزماییِ بیلد همیشه `npm run verify-build` بزن** (روی `.next-verify` می‌سازد و به `.next`ِ سرورِ dev دست نمی‌زند). **هرگز `npm run build`/`next build` را در حالِ اجرای `npm run dev` نزن** و `.next` را زیرِ سرورِ در حال اجرا پاک نکن — پوشه مشترک است و سرورِ dev با `Cannot find module`/`ENOENT`/۵۰۰ می‌افتد. اگر اشتباهاً build زدی، سرور را kill و تازه راه‌اندازی کن (`pkill -f "next dev"; rm -rf .next; npm run dev`). داده در `data/` مستقل است و با پاک‌کردنِ `.next` از بین نمی‌رود؛ پس از راه‌اندازیِ تازه، `npm run seed`

import { ChatSession } from '../_components/chat-session';

export default function NewConversationPage() {
  return (
    <ChatSession
      title="گفتگوی تازه"
      intro="پرسشت را بنویس — به هر زبانی که با آن می‌اندیشی. مناد عجله ندارد."
    />
  );
}
